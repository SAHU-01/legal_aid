{
  "name": "legal-aid-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "ZK circuits for selective credential disclosure",
  "scripts": {
    "build": "./build.sh",
    "setup": "./setup.sh",
    "clean": "rm -rf build/"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
